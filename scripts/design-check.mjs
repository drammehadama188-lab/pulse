#!/usr/bin/env node
// `npm run design-check` — the rules live in ONE place: test/design-rules.test.mjs.
// This is only a shortcut to it, so there is never a second rulebook to drift from.
import './../test/design-rules.test.mjs'
